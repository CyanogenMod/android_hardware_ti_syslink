Need to Know before starting autotools:
=======================================
	Tools Prequisites:
	------------------
	1. Autoconf version: 2.61 and above.
	2. Automake version: 1.9.6 and above.
	3. libtool version : 1.5.6 and above.

	ENABLING DEBUG OPTION:
	----------------------
	1. You can enable the DEBUG option in build_directions.sh by uncommenting
	   line number 56.
	2. You can also enable the DEBUG option by entering ./configure --enable-debug.
	   Note: please check for ./configure --help before running ./configure.

Steps to Build syslink & bridge:
================================
1st Method:
----------
	Build directions:
	-----------------
	1. sh build_directions.sh
	2. Enter the Prefix path ( e.g., /omap_data/development/Imtiaz/omap_dev/userspace-syslink)
	3. Enter the Tiler-Userspace Path  (e.g., /omap_data/development/Imtiaz/omap_dev/tiler-userspace)
	4. Enter the Toolchain Path ( e.g., /omap_data/omapts/arm-2008q3-72/bin/arm-none-linux-gnueabi-gcc )
	5. First the script will build the libmemgr.so and install it in $(PREFIX)/target/lib folder.
	6. Second the Syslink APIs, daemons and samples will be built and installed in $(PREFIX)/target/lib
	   $(PREFIX)/target/syslink folder respectively.
	7. Third the bridge APIs and samples will be built and installed in $(PREFIX)/lib
	   $(PREFIX)/target/dbpbridge folder respectively.


2nd Method:
----------
	Prequisites:
	------------
	export ARCH=arm
	export PATH=/omap_data/omapts/arm-2008q3-72/bin:$PATH
	export CROSS_COMPILE=arm-none-linux-gnueabi-
	export CC=arm-none-linux-gnueabi-

	Then follow the below steps:
	To build only syslink:
	---------------------
	1. cd syslink
	2. sh autogen.sh
	3. ./configure --host=arm-none-linux-gnueabi- --prefix=<PREFIX PATH> --build=i686-pc-linux-gnu
	4. make TILER_INC_PATH=/omap_data/development/Imtiaz/omap_dev/tiler-userspace/memmgr
	5. make install
	6. cd -

	To build only bridge:
	---------------------
	1. cd bridge
	2. sh autogen.sh
	3. ./configure --host=arm-none-linux-gnueabi- --prefix=<PREFIX PATH> --build=i686-pc-linux-gnu
	4. make
	5. make install
	6. cd -

Steps to build only the syslink APIs:
=====================================
	Prequisites:
	------------
	export ARCH=arm
	export PATH=/omap_data/omapts/arm-2008q3-72/bin:$PATH
	export CROSS_COMPILE=arm-none-linux-gnueabi-
	export CC=arm-none-linux-gnueabi-
	Then follow the below steps:
	1. cd syslink
	2. sh autogen.sh
	3. ./configure --host=arm-none-linux-gnueabi- --prefix=<PREFIX PATH> --build=i686-pc-linux-gnu
	4. cd api
	5. make clean
	6. make
	7. make install
